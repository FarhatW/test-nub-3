// import { Pipe, PipeTransform } from '@angular/core';
// import {PintelSheet} from '../../data/models/pintelSheet';
//
// @Pipe({
//   name: 'filterSheet',
//   pure: true
// })
//
// export class FilterPipe implements PipeTransform {
//   transform(items: any[], filter: PintelSheet): any {
//     if (!items || !filter) {
//       return items;
//     }
//     return items.filter(item => item.name.indexOf(filter.id) !== -1);
//   }
// }
